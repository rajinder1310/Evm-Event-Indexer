# Database Connection
MONGO_URI=mongodb://localhost:27017/evm_indexer

# Optional: Default RPC URL (if used for fallback or single chain testing)
# RPC_URL=https://mainnet.infura.io/v3/YOUR-KEY

# Server Port (if we add an API later)
PORT=3000
